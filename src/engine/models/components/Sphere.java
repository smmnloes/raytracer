package engine.models.components;

import engine.util.Intersection;
import engine.util.Vector3D;

public class Sphere extends Geometry {
    @Override
    public Intersection intersects(Vector3D ray) {
        return null;
    }
}
